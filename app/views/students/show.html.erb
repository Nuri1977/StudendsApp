<div class="row">
  <div class="col s6 m6">
    <div class="card blue-grey darken-1">
      <div class="card-content white-text">
        <span class="card-title"><%= @student.name %></span>
        <span class="card-title"><%= @student.email %></span>
        <p>Corse enrollmets : None </p>
      </div>     
    </div>
    <%= link_to '<i class="material-icons right">create</i>Edit profile'.html_safe, edit_student_path(@student), class: "waves-effect waves-light btn" %>
    <%= link_to '<i class="material-icons right">create</i>Delete profile'.html_safe, student_path(@student), method: :delete, class: "waves-effect waves-light btn" %>
  </div>
</div>